# Test Service Backend Code

This directory contains the Python package and its local development environment.

Run `asdf install` before `uv sync --extra dev` to use the repository-pinned
Python and uv versions.
